---
name: e_commerce

networks:
  ecommerce:

services: 

  database:
    image: postgres:14-alpine
    restart: unless-stopped
    ports:
      - 5432:5432
    networks:
      - ecommerce 
    env_file:
      - path: ./app/.env.prod
        required: true
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U symfony"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 20s

  php_fpm:
    build:
      # The Dockerfile should be located at the root of the project
      context: .
    env_file:
      - ./app/.env.prod
    ports:
      - 9000:9000
    networks:
      - ecommerce 
    depends_on:
      - database
    healthcheck:
      # Makes the migrations the -q flag returns a status code 
      test: ["CMD", "make", "migrate", "-q"]
      interval: 5s
      timeout: 30s
      retries: 5
      start_period: 30s

  nginx:
    image: nginx:1.25.1
    depends_on:
      - php_fpm
    ports:
      - 1030:80
    volumes:
      # Mount our Nginx configuration as the default conf
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      # Mount the frontend application
      - ./front/build/:/var/www/e_commerce/
    networks:
      - ecommerce 

  
